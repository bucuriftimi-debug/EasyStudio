# Totul rămâne pe E: — nimic în cache-urile implicite de pe C:
# (cache-ul npm, Electron, electron-builder și TEMP sunt setate de env.ps1 — rulează-l înainte de npm)
fund=false
audit=false
update-notifier=false
